---
- name: build environment
  hosts: localhost
  become: true
  gather_facts: true
  roles:
    - build_env
  tags:
    - build

- name: deploy artifacts
  hosts: localhost
  become: true
  gather_facts: true
  roles:
    - deployment
  tags:
    - deploy
